# Parlay Lab Science Icons

Single-color original monoline SVG set. 64x64 viewBox, #071F33 stroke, 3px rounded stroke, transparent background. Use `science.svg` for Parlay Lab identity; `experiment.svg` for AI/experiments; `test-tube.svg` for Lab Finds; `data.svg` for data; `parlay.svg` for parlay UI.
