name = "dnd-content-scheduler"
main = "src/index.js"
compatibility_date = "2026-09-07"

# Intentionally no cron trigger here. Add `*/5 * * * *` only after the Vercel
# endpoint, database migration, kill switch, and encrypted Worker binding have
# been reviewed and tested.
